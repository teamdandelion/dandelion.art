#!/bin/sh

# Run the framework's production build, including its compile-time checks.
echo "🔍 Checking production build..."
npm run build || exit 1

# Run lint-staged for formatting
echo "✨ Running formatters..."
npx lint-staged
